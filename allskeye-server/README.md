Allsky computer server to open websocket connections to get latest all sky images in real time for the view client.
Also acts as relay to interface with the AllSky ESP32 microcontroller. Can bounce update requests to the ESP32 from
outside local network and keeps an auto updating buffer of the ESP32's state variables for use in the view client.
